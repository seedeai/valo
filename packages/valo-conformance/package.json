{
  "name": "valo-conformance",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --config vitest.config.ts",
    "test:curated": "vitest run --config vitest.config.ts tests/curated.browser.test.ts",
    "test:fuzz": "vitest run --config vitest.config.ts tests/fuzz.browser.test.ts",
    "benchmark": "vitest run --config vitest.benchmark.config.ts"
  },
  "dependencies": {
    "valo-web": "0.1.0"
  },
  "devDependencies": {
    "@blazediff/core": "^1.9.1",
    "@types/pngjs": "^6.0.5",
    "@vitest/browser-playwright": "^4.0.0",
    "fast-check": "^4.0.0",
    "playwright": "^1.55.0",
    "pngjs": "^7.0.0",
    "typescript": "^5.9.2",
    "vitest": "^4.0.0"
  }
}
