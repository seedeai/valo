{
  "name": "@valo/web",
  "version": "0.1.0",
  "description": "WebGPU-native 2D rendering with a retained API and Canvas2D adapter",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tyxu/valo.git",
    "directory": "packages/valo-web"
  },
  "homepage": "https://github.com/tyxu/valo#readme",
  "keywords": ["canvas", "graphics", "webassembly", "webgpu"],
  "type": "module",
  "files": ["dist", "wasm"],
  "publishConfig": {
    "access": "public"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./raw": {
      "types": "./dist/raw.d.ts",
      "import": "./dist/raw.js"
    }
  },
  "scripts": {
    "build:wasm": "wasm-pack build ../../crates/valo-web --target web --out-dir ../../packages/valo-web/wasm --out-name valo_web --no-pack && node -e \"require('node:fs').rmSync('wasm/.gitignore', { force: true })\"",
    "build": "npm run build:wasm && tsc -p tsconfig.json",
    "check": "npm run build:wasm && tsc -p tsconfig.json --noEmit && tsc -p playground/tsconfig.json",
    "dev": "npm run build:wasm && vite playground",
    "build:playground": "npm run build && vite build playground",
    "prepack": "npm run build"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vite": "^7.1.2"
  }
}
