/** The lime blob and the wordmark, as they appear in the playground's topbar. */
export function Brand() {
  return (
    <span className="flex items-center gap-2.5 text-[22px] font-semibold leading-none tracking-[-0.04em]">
      <span className="valo-mark" aria-hidden />
      valo
    </span>
  );
}
